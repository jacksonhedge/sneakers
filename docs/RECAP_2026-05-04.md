# Sneakers Terminal — Session Recap (2026-05-03 → 02:45 EDT 2026-05-04)

## Started with

"OG markets aren't showing on prod" — last night's session got cut off mid-thread.

## Ended with

OG live, dashboard ~17x faster on cold-start, 4 silent bugs fixed, ~30 commits worth of value in 9 ships.

---

## What shipped (in order)

**Path A: get OG live**
- Diagnosed prod was serving 13 hardcoded seed markets — Vercel `POSTGRES_URL` pointed at NXDOMAIN host `base...` + `SNEAKERS_ENABLE_SEED=1`
- Repointed Vercel at Railway public TCP proxy (`shortline.proxy.rlwy.net:18882`), flipped seed flag to 0, redeployed
- 191k markets live in seconds

**Supabase migration drift**
- Two 42703 errors traced to missing columns: `waitlist.subscription_status` (mig 007) + `user_provider_keys.label` (mig 009)
- Both columns were in committed migrations but never applied to prod Supabase
- Idempotent ALTERs ran cleanly, errors stopped

**OG in dashboard chrome** (`232f384`)
- Apps-bar featured row: added OG between Kalshi and NoVig
- Footer venue list updated
- NotAdminBanner shipped (was sitting uncommitted from prior session)

**Chart history flat-line bug** (`4b80219`)
- Every OG/NoVig/ProphetX market showed "2 pts · still loading" because `loadMarketHistory(7)` had `LIMIT 200_000 ORDER BY market_id` — Kalshi alone consumed the cap, alphabetically later platforms got nothing
- New `loadSingleMarketHistory()` per-market indexed seek
- OG charts went from 2 pts → real curves (83 pts on AA Estudiantes, 262 on ProphetX, 131 on NoVig)

**Cold-start: 12-15s → 702ms** (`5bd4251`)
- Bottleneck: `loadCanonicalMarkets()` pulled 376K rows / 125 MB on every cold function instance to compute groups in-memory
- Added `markets.canonical_id` column to Railway, backfilled 189,536 markets / 24K cross-venue groups
- New `loadCanonicalForMarket()` does indexed seek (~80ms)
- Recompute job runs every Railway scrape iteration (~11s)
- 17-21x faster cold, 25-35x faster warm

**Landing page** (`eb19c4d`)
- `/` only used `marketCount = snapshots.length` but paid the full 5s pull
- New `loadMarketCount()` count query: 96ms vs 4,765ms

**Cache dedup** (`6983700`) — wrapped `loadAllLatestSnapshots` in React `cache()` so dashboard's parallel callers stop firing duplicate 376K-row pulls

**Cross-book pairs precompute + silent starts_at bug** (`48d1c76`)
- Discovered: scraper never persisted `starts_at` to DB, only to JSONL. `findCrossBookPairs` filters out anything without `starts_at` → silently returned 0 pairs from prod for who-knows-how-long
- Fixed at write layer (db-write.ts), read layer (dbRowsToSnapshot), and added `cross_book_pairs` precompute table

**Observability + footer fix** (`545197a` + `133fdb6`)
- Added slow-query (>3s) + failure-with-SQL logging to `safeQuery()`
- Footer switched to use `loadMarketCount()` after marketsResult.total returned 21K instead of 191K

**Polymarket scraper unblock** (`ebb99b0`)
- Was timing out at exit 124 after 5 min — sequential per-market orderbook fetches
- Batched at concurrency=10 → completes in ~3 min, no timeout
- Cascade: polymarket markets re-enter the 24h fresh window

**Drop wasted ORDER BY** (`426aff5`)
- `recompute-arb-pairs` was failing with `BufFileDumpBuffer` (Railway pgsql_tmp full from external sort)
- `/dashboard` started showing function-timeout `---` status
- Removed ORDER BY from 3 queries — consumers group by market_id in JS, sort was wasted
- Crashes fixed; perf benefit didn't materialize (query is dominated by LATERAL JOIN + network, not sort)

---

## Live now

- OG markets flowing
- 4 charts populating with real history (no more "still loading" lies)
- Cold-start under 1s
- Footer accurate at 197K markets
- Polymarket scraping cleanly
- Zero pgsql_tmp warnings

## Open for next session

1. **Cross-Book Spread panel still empty** — `recompute-arb-pairs` runs in 6s with no error but produces 0 rows. Needs `console.log` observability inside `findCrossBookPairs` to find which gate (10-min skew, pre-game phase, team naming overlap) drops everything. Memory saved at `project_cross_book_pairs_empty.md`.
2. **/dashboard still 6.8s** — query is dominated by 58K wide rows, not the sort. Next move: column projection or precomputed dashboard payload.
3. **Polymarket 161s** vs predicted 30s — bottleneck moved upstream of orderbook fetches.
4. **opinion scraper exit 1** (pre-existing)
5. **CRON_SECRET unset** on preview cron auth (pre-existing)

## Memory updated

New entries: `project_prod_db_path_a`, `feedback_vercel_sensitive_var_snapshot_drift`, `feedback_no_credential_rotation_nagging`, `project_supabase_migration_drift`, `project_canonical_id_precomputed`, `project_railway_pgsql_tmp_pressure`, `project_dashboard_24h_freshness_filter`, `project_cross_book_pairs_empty`.

Future sessions will load these automatically.
